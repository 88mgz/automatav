/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: { ignoreDuringBuilds: false },   // set true only if you need a bypass
  // typescript: { ignoreBuildErrors: true },
};
export default nextConfig;
